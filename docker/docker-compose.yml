version: '3.3'
services:
  post_db:
    image: mongo:${MONGO_VERSION}
    volumes:
      - post_db:/data/db
    networks:
      back_net:
        aliases:
          - post_db
          - comment_db
      monitoring_net:
  ui:
    image: ${USER_NAME}/ui
    ports:
    #  - ${EXT_PORT}:9292/tcp
      - 9292:9292/tcp
    networks:
      - front_net
      - monitoring_net
  post:
    image: ${USER_NAME}/post
    networks:
      - back_net
      - front_net
      - monitoring_net
  comment:
    image: ${USER_NAME}/comment
    networks:
      - back_net
      - front_net
      - monitoring_net

volumes:
  post_db:

networks:
  back_net:
  front_net:
  monitoring_net:
